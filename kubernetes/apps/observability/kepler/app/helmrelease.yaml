---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kepler
spec:
  chartRef:
    kind: OCIRepository
    name: kepler
  interval: 1h
  values:
    config:
      log:
        level: info
    fullnameOverride: kepler
    daemonset:
      resources:
        limits:
          cpu: 3000m
          memory: 128Mi
        requests:
          cpu: 160m
          memory: 128Mi
    podSecurityContext:
      runAsUser: 2000
      runAsGroup: 2000
      fsGroup: 2000
      fsGroupChangePolicy: OnRootMismatch
      seccompProfile:
        type: RuntimeDefault
    serviceMonitor:
      enabled: true
      interval: 15s
      scrapeTimeout: 10s

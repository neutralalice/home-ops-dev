---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app kaniop
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: *app
  values:
    deploymentAnnotations:
      reloader.stakater.com/auto: "true"
    env:
      - name: LISTEN_ADDRESS
        value: "::"
    logging:
      format: json
      level: info
    webhook:
      enabled: true
      env:
        - name: LISTEN_ADDRESS
          value: "::"
      logging:
        format: json
        level: info
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
      dashboards:
        enabled: true
